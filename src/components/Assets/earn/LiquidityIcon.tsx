/* eslint-disable no-empty-pattern */
import { Icon, type IconProps } from "@chakra-ui/react"
import { RiWaterFlashFill } from "react-icons/ri"

export const LiquidityIcon = ({}: IconProps) => (
	<Icon boxSize={9} pt={2}>
		<RiWaterFlashFill />
	</Icon>
)
